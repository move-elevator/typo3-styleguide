services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false

    MoveElevator\Styleguide\:
        resource: '../Classes/*'
        exclude:
            - '../Classes/Domain/Model/*'
            # Registered conditionally in Services.php — depends on hn/typo3-mcp-server.
            - '../Classes/Mcp/*'
